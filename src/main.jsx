import React from 'react'
import ReactDOM from 'react-dom/client'
import TradeDashboard from './components/TradeDashboard.jsx' // 确保路径正确

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TradeDashboard />
  </React.StrictMode>,
)